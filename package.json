{
  "name": "table-football",
  "private": true,
  "scripts": {
    "bootstrap:client": "cd client && yarn && yarn start",
    "start:client": "cd client && yarn start",
    "start:server": "cd server && denon run --allow-net --allow-read --allow-write --unstable main.ts"
  }
}
